[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn 'apps.web.main:create_app()' --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/state"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
